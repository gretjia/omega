我已经把你的模型整理成一份**讯投投研端内置 Python 可直接执行**的策略脚本（已解决你上传的 `kernel.py/config.py` 中存在的 `...` 占位符导致的“不可运行”问题），并保留了你“参数与算法”的**三处插拔接口**，方便你交给另一个 AI 自动生成/替换核心逻辑。

[下载 omega_v1300_qmt_strategy.py](sandbox:/mnt/data/omega_v1300_qmt_strategy.py)

### 你接下来怎么用（最少步骤）

1. 把这个文件放到投研端策略脚本目录（与其他策略 .py 同目录即可）。
2. 打开文件，填入你的 **157 只股票池**（脚本顶部 `STOCK_POOL` 可扩展；当前模板以“主图标的”运行，便于先跑通）。
3. 在投研端回测界面选择周期为 **tick**（或你的版本支持的 `l2transaction/l2order`），并确保历史数据已下载到本地。
4. 运行回测。

### 你要交给 AI 写回测代码时，应该让它替换的地方

脚本里明确留了三处函数让 AI “填真正模型”：

* `build_features_from_ticks(...)`：从 tick/逐笔窗口生成基础特征
* `score_state(...)`：生成 `OmegaState`（z_k / z_p / kappa / defect / energy_gap 等）
* `decide_orders(...)`：入场/出场/止损止盈/仓位控制

如果你把你“真实的参数与算法说明”（比如 z_k/z_p/kappa/defect 的精确定义、门控条件、仓位与风控规则）贴出来，我可以再把这份脚本进一步“落地成你模型的完全等价实现”，并把 **157 股票池 + L2 批量下载 + tick 回测**也集成成一套可直接跑的工程结构。
