# v40 P1 Fix Closure (2026-02-08)

## Scope
Close P1 findings from `audit/v40_independent_audit_2026-02-08.md`:
1. backtest hidden hard-coded return clip
2. prior-learning non-determinism

## Fix 1: Backtest return clip de-hardcoded

### Changes
- `config.py`
  - `L2ValidationConfig.backtest_ret_clip_abs: Optional[float] = None`
  - `load_l2_pipeline_config(...)` supports `AUTO_LEARNED_PARAMS.BACKTEST_RET_CLIP_ABS`
- `parallel_trainer/run_parallel_backtest_v31.py`
  - removed fixed `np.clip(ret_k, -0.02, 0.02)`
  - clip now resolved by precedence:
    1. CLI `--ret-clip-abs`
    2. policy cfg `validation.backtest_ret_clip_abs`
    3. None (no clipping)
  - status JSON now exposes `ret_clip_abs`
  - final report prints `Ret Clip Abs`

### Validation
- no-clip smoke: `ret_clip_abs = null` in status JSON
- override smoke: `ret_clip_abs = 0.02` in status JSON

## Fix 2: Prior-learning reproducibility

### Changes
- `config.py`
  - `L2EpiplexityConfig.prior_random_seed: int = 42`
- `omega_v3_core/physics_auditor.py`
  - deterministic RNG helper `_rng(salt)`
  - deterministic sampling/shuffle in:
    - `derive_market_priors`
    - `_sample_files`
    - `_generate_epistemic_report`
  - prior export now includes:
    - `AUTO_LEARNED_PARAMS.PRIOR_RANDOM_SEED`
    - `PRIORS_AUDIT.PRIOR_SAMPLE_FILES`
    - `PRIORS_AUDIT.PRIOR_SAMPLE_LIST`

### Validation
- repeated `derive_market_priors()` calls return identical outputs on same data
- `production_config.json` contains deterministic-seed and sampled-file evidence

## Sanity Gates
- `./.venv/bin/python -m py_compile config.py omega_v3_core/physics_auditor.py parallel_trainer/run_parallel_backtest_v31.py` -> PASS
- `./.venv/bin/python tools/v40_recursive_audit.py` -> PASS

## Result
- P1-1 resolved: hard-coded backtest clip removed from code path.
- P1-2 resolved: prior-learning path now deterministic and auditable.
