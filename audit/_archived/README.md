# Runtime Current

This directory is the version-agnostic runtime workspace used by top-level documentation.

Use it for:
- `run_meta.template.json`
- shard manifests (`archive_manifest_7z.txt`, `shard_windows1.txt`, `shard_linux.txt`)

Historical runtime folders may keep versioned names for archive compatibility, but new docs should point here.
