# OMEGA vNext Handover (2026-02-05) �� Task 2: Rebuild L2 Frames Under v3.1

## Request
User chose option 2 from `audit/v31_training_prep.md`: rebuild Level-2 frames under the v3.1 standard.

## What I did
- Read and analyzed the following files:
  - `audit/v31_training_prep.md`
  - `audit/v3_training_preparation_plan.md`
  - `audit/20260205_handover.md`
  - `omega_v3_core/README.md`
  - `omega_v3_core/omega_etl.py` (confirmed `build_l2_frames` generates `trade_vol`, `cancel_vol`, `lob_flux`)
  - `omega_v3_core/kernel.py` (confirmed `apply_recursive_physics` computes `spoof_ratio`, `epiplexity`, `topo_*`, `srl_resid`, `is_signal`)
  - `tools/run_l2_audit_driver.py` (driver that extracts `.7z`, runs kernel, writes parquet frames)
  - `tools/level2_batch_extract.md` / `tools/level2_batch_extract.py` (manual batch extraction; not required if using driver)
  - `tools/verify_v3_data.py`, `tools/check_v3_compatibility.py` (post-rebuild validation)
  - `tools/multi_dir_loader.py`, `config.py` (defaults + input root `./data/level2`)
- Verified raw archives exist at `d:\Omega_vNext\data\level2\2023` and `d:\Omega_vNext\data\level2\2024`.
- Verified 7-Zip is installed at `C:\Program Files\7-Zip\7z.exe`.
- Attempted to add a `--skip-report` flag to `tools/run_l2_audit_driver.py` to avoid full-report memory cost on huge rebuilds. The patch was aborted by user; no code changes were applied.

## Key findings
- Current frames in `data/level2_frames_*` are missing v3.1-required fields (`trade_vol`, `cancel_vol`, `spoof_ratio`, `lob_flux`). This matches `audit/v31_training_prep.md`.
- v3.1 frame generation is already implemented in `omega_v3_core/omega_etl.py` + `omega_v3_core/kernel.py`.
- `tools/run_l2_audit_driver.py` is the simplest end-to-end rebuild path: it extracts `.7z` archives, runs `run_l2_kernel`, and writes per-day parquet frames.
- Driver default behavior generates an aggregate report by loading *all* rebuilt parquet files into memory. That is likely too heavy for a full rebuild.

## Expected next steps for the next AI coder
1. Rebuild frames for 2023 and 2024 using the driver (full rebuild):
   - From `d:\Omega_vNext`:
     - `python tools/run_l2_audit_driver.py --year 2023 --limit 0 --workers 4 --output-dir data/level2_frames_win2023 --copy-to-local`
     - `python tools/run_l2_audit_driver.py --year 2024 --limit 0 --workers 4 --output-dir data/level2_frames_mac2024 --copy-to-local`
   - Notes:
     - `--limit 0` means no limit; default is `1`.
     - `--copy-to-local` improves performance if archives are on slower media.
     - If you keep report generation on, expect heavy memory usage at the end.
2. Validate rebuilt frames:
   - `python tools/verify_v3_data.py --dir data/level2_frames_win2023`
   - `python tools/verify_v3_data.py --dir data/level2_frames_mac2024`
   - `python tools/check_v3_compatibility.py`
3. Update audit docs if needed (e.g., new report file or rebuild notes).

## Risks / gotchas
- `tools/run_l2_audit_driver.py` will attempt to load all parquet files to compute a final report. This can be too heavy for full rebuilds; consider patching to skip or generate report on a sampled subset.
- Output directory is **not** year-specific by default; be sure to pass `--output-dir` for 2023 and 2024 to avoid mixing.

## No code changes applied
- No files were modified. The `--skip-report` patch attempt was aborted before execution.
