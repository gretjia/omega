Report written to:

`audit/runtime/v52/incidents/20260217_163802_autopilot_status_stale.codex_report.txt`

Outcome:
- Root cause is a stale-status false positive during long/silent `build_base_matrix`, not a crashed autopilot.
- Verified with live process evidence (`tools/v60_autopilot.py` PID 26005, child `tools/v60_build_base_matrix.py` PID 26930, uplink loop PID 38903), stale status/log mtimes, and watchdog stale-trigger logic.
- No restart/kill was performed (minimal safe intervention) to avoid duplicating heavy compute.

Current status:
- Pipeline is still running in `build_base_matrix` and has not crashed.
- Status JSON remains stale by design of this stage (no heartbeat output until later stage transitions).

Recommended immediate checks (already listed in the report):
1. Watch for `artifacts/runtime/v60/20260217-074618_aa8abb7/base_matrix.parquet` and `.meta.json`.
2. Watch runner log for transition to `post_base_matrix` and `vertex_optimize`.