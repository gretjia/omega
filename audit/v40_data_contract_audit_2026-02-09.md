# v40 Data Contract Audit (Frame -> Train/Backtest)

Date: 2026-02-09  
Scope: verify frame output contract alignment with v40 Patch-02 and train/backtest runtime requirements.

## Baseline Policy (from code)
- `config.py` split policy:
  - train years: `2023, 2024`
  - backtest years: `2025`
  - backtest year-months: `202601`

## Verification Command
```bash
python3 tools/verify_v40_data_contract.py
```

## Result
- **PASS** (contract aligned)
- Evidence from runtime artifacts under `audit/v40_runtime/windows/`:
  - split preflight completed
  - train/backtest manifests built and disjoint
  - manifest counts match status JSON
  - train runtime status completed with zero schema/worker errors
  - backtest runtime currently running with zero file errors

## Important Note
- Current `frame_compat_status.json` was generated by an older checker output schema and does not contain `checks.close_positive_guard`.
- This is not a split-contract failure, but strict Patch-02 close-guard evidence should be refreshed once on Windows.

## Strict Recheck (Windows, recommended)
```powershell
cd D:\Omega_vNext
python tools\check_frame_train_backtest_compat.py `
  --input-dir data\level2_frames_v40_win `
  --sample-files 96 `
  --prepare-smoke-files 3 `
  --status-json audit\v40_runtime\windows\frame\frame_compat_status.json
python tools\verify_v40_data_contract.py --strict-close-guard
```

Expected strict result: PASS.
