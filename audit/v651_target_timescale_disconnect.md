# System Architect Recursive Audit Verdict: The Target Timescale Disconnect

Status: Frozen external audit authority
Date: 2026-03-09
Scope: Frozen V64 canon + full Path A evidence chain + Path B diagnosis + V650 zero-mass gravity-well result

## Central Claim

Verdict:

- `PARTIAL ONLY`

Interpretation:

- the V64 math canon still successfully isolates the physical footprint of `Epiplexity`,
- but stable downstream `intelligence` remains unproven,
- because the current learner/label interface still collapses before it can map physical compression into durable economic direction.

## Short Verdict

V650 proves that swapping the regression loss from standard L2 to a robust loss does not solve the current Path B collapse.

The branch still degenerates into flat predictors under the frozen raw `t1_excess_return` target:

- `audit/runtime/v650_local_sweep_20260309_133400/worker_local/study_summary.json`
- `handover/ai-direct/entries/20260309_133613_v650_local_robust_loss_kill_condition_triggered.md`

Therefore the blocker is no longer:

- the learner family by itself,
- or the regression loss geometry by itself.

The blocker is the target-timescale definition:

- the V64 physics extracts a macroscopic compression process,
- but the learner is still forced to decode it through a one-step microscopic release target.

## Ranked Root Causes

1. **Target Timescale Disconnect**
   - the physical signal is macroscopic and accumulative,
   - the current target contract is still one-step and too short,
   - this mismatch suppresses the release process into a highly zero-dominated label.
2. **Zero-Mass Dominance**
   - the `t1` continuous target remains so zero-heavy that even robust loss prefers dead-zone predictions.
3. **Path A Exhaustion**
   - the binary bypass is already exhausted and cannot be reopened as the leading branch.

## Evidence-Backed Reasoning Path

### Physics signal exists

- `handover/ai-direct/entries/20260309_084315_v645_path_a_retrain_and_fresh_holdout_partial_pass.md`

This proves the frozen V64 features exert real out-of-sample leverage when the zero-mass continuous target is bypassed.

### Path A is exhausted

- `audit/v647_anti_classifier_paradox.md`
- `audit/v648_path_a_collapse_anti_classifier_paradox.md`

These establish that binary classification cannot safely serve as the long-term translation layer for continuous fat-tail outcomes.

### Path B baseline collapsed

- `audit/v649_path_b_flat_predictor_diagnosis.md`

This isolates the first continuous-regression failure:

- raw `t1_excess_return` is heavily zero-dominated,
- the first regression regime can collapse into a constant predictor,
- simple variance recovery is still not enough to recover structural ranking.

### Robust loss did not rescue the branch

- `audit/v650_zero_mass_gravity_well.md`
- `audit/runtime/v650_local_sweep_20260309_133400/worker_local/study_summary.json`

These show that even `reg:pseudohubererror` stays trapped in the same flat-predictor basin under the raw `t1` target.

### Conclusion

If:

- Path A is structurally exhausted,
- Path B with standard regression failed,
- and Path B with robust regression also failed,

then the next bounded axis is no longer the learner geometry.

It is the target horizon.

## Convergence Plan

Freeze:

- `omega_core/*`
- `canonical_v64_1` Stage3 gates
- Path B continuous regression intent
- train / validation split:
  - `2023`
  - `2024`
- holdout isolation:
  - `2025`
  - `2026-01`
- the explicit non-degeneracy gates introduced in the sweep path

Change one axis:

- expand the target horizon beyond `t1`

Preferred bounded first ladder:

- `t5`
- `t10`
- `t20`

which correspond approximately to:

- 1 trading week
- 2 trading weeks
- 4 trading weeks

Metrics that must improve together:

- target zero-mass should dilute materially versus `t1`
- `val_pred_std` must escape zero
- non-zero feature-importance count must be greater than zero
- `val_spearman_ic` must become distinctly positive

Immediate kill condition:

- if a bounded multi-horizon branch still yields flat prediction or non-positive rank correlation across the ladder,
- then the raw continuous tree-regression interface should be considered exhausted,
- and the next mission must become a deeper target-transformation or learner-family redesign.

## Concrete Next Mission Narrowing

Mission name:

- `V651 Target Timescale Alignment Pivot`

Mission shape:

- keep Path B as the leading branch
- keep the learner family and non-degeneracy guardrail fixed
- change only the target horizon
- build a new train-only target-expanded matrix contract
- run local bounded sweeps for separate fixed horizons:
  - `t5`
  - `t10`
  - `t20`

This mission should not:

- reopen Path A
- reopen cloud
- consume holdouts in wave 1
- touch `omega_core/*`
- mix horizons into one blended target

Each horizon must be evaluated as a separate fixed contract.
